-- ============================================================================
-- tasks 테이블에 phase(양수도 진행 단계 1~5) 컬럼 추가
-- ----------------------------------------------------------------------------
-- 초기 schema.sql 을 이미 실행한 환경(기존 Supabase 프로젝트)에서는 이 한 줄만
-- SQL Editor 에 추가로 실행하면 됩니다. (새로 schema.sql 을 실행하는 경우 불필요)
-- ============================================================================

alter table tasks add column if not exists phase smallint;
